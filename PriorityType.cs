namespace Assignment_06
{
    /// <summary>
    /// Enum <c>PriorityType</c> defines the available task priority levels that
    /// the user can select from.
    /// </summary>
    public enum PriorityType
    {
        Very_important,
        Important,
        Normal,
        Less_important,
        Not_important
    }
}
